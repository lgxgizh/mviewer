#include "core/command/CallbackCommand.h"

// CallbackCommand is fully defined inline in its header; this translation
// unit exists only so the build system has a concrete source to compile and
// so the symbol is emitted in mviewer_core (WINDOWS_EXPORT_ALL_SYMBOLS).
