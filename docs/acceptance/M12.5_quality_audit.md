# M12.5 — Quality Audit

**Milestone**: M12 Product Beta Hardening → M12.5 Quality Audit
**Date**: 2026-07-19
**Scope**: (5.1) Qt-boundary scan of core/domain headers; (5.2) thread-safety audit
of the four singleton core services; (5.3) API documentation skeleton for the
public core surface.

**Freeze note**: This audit is *read-mostly*. It documents the current state and
fixes only mechanical, low-risk boundary issues. No architecture change, no
refactor of `ImageRepository` / `CacheManager` / `RenderEngine` (per AGENTS.md
freeze). Two Qt-in-core headers are confirmed **sanctioned boundary points**,
not leaks.

---

## 5.1 Qt-boundary scan (core/domain headers)

**Rule** (AGENTS.md, as corrected by `docs/adr/016-qt-boundary-in-core-headers.md`):
*no Qt types in `domain/` headers, and core headers must not `#include` Qt — the
two named adapters (`core/image/QtConvert.h`, `core/image/QtMetadataSemantics.h`)
are the only exception, enforced by the R5 check in
`scripts/architecture_gate.ps1`.*

**Tool**: `scripts/audit_qt_boundary.ps1` (additive; runs in CI or locally).
Scans `src/core/**/*.h` + `src/domain/**/*.h` for forbidden UI includes
(`QWidget`, `QPainter`, `QPixmap`, `QMainWindow`, `QGraphicsView`, …) and warns
on Qt GUI value types (`QImage`, `QPicture`). It reads files with the default
(ANSI) encoding, so on UTF-8-without-BOM files its printed line numbers drift
(it reports `QtConvert.h:8` for an include that is on line 11); the
authoritative check is **R5** in `scripts/architecture_gate.ps1`, which reads
UTF-8.

**Result (138 headers scanned — counts regenerated at HEAD)**:

| File | Qt include in the header | Verdict |
|------|--------------------------|---------|
| `src/core/render/RenderEngine.h` | none — line 5 is a comment; `class QPainter; class QRect;` are forward declarations (lines 12-13) | **CLEAN** — not an R5 exception (see below) |
| `src/core/image/QtConvert.h` | `<QImage>` (line 11) | **SANCTIONED** — R5 adapter #1 (see below) |
| `src/core/image/QtMetadataSemantics.h` | `<QImage>` (line 10) plus `<QColorSpace>`, `<QImageIOHandler>`, `<QImageReader>` (lines 9-12) | **SANCTIONED** — R5 adapter #2 (the scanner warns on it — see below) |
| all other 135 headers | — | **CLEAN** (0 forbidden, 0 warnings) |

**The two sanctioned Qt adapters — and the header that is *not* one:**

1. ~~`RenderEngine.h`~~ — **not a Qt exception of any kind.** That header
   includes *no* Qt header at all: line 5 is a comment, and `class QPainter;
   class QRect;` (lines 12-13) are forward declarations, so an
   `#include <Q...>` scan cannot hit it and R5 does not need to allow it. The
   two headers R5 allow-lists are the adapters below, and only those. What made
   the old table misleading is that the header is not Qt-*free* either: its
   public entry point `executeCommand(QPainter &, const RenderCommand &, const
   QRect &)` keeps `QPainter&`/`QRect` in the API (callers must already have Qt).
   That is exactly the distinction documented in
   `docs/adr/016-qt-boundary-in-core-headers.md`: core/domain headers must not
   *include* Qt, which is not the same claim as "core has no Qt in its API".
   `RenderEngine` is frozen (AGENTS.md), so this split stays.
2. `QtConvert.h` — the explicit `ImageBuffer ↔ QImage` conversion adapter, and
   the first of the two named R5 adapters. By design this is *a* point where Qt
   enters core; isolating the conversion here keeps every other core header
   Qt-include-free.
3. `QtMetadataSemantics.h` — the second named R5 adapter (colour-space /
   raster metadata semantics: `<QColorSpace>`, `<QImageIOHandler>`,
   `<QImageReader>`). `scripts/audit_qt_boundary.ps1` still reports it as a
   warning because that scanner's older allow-list names `RenderEngine.h` +
   `QtConvert.h` instead; the authoritative R5 check allows it.

**Conclusion**: the architecture boundary is intact and R5-clean. At HEAD the
scanner exits 0 with 0 forbidden hits and exactly 1 warning
(`QtMetadataSemantics.h`), and `scripts/architecture_gate.ps1` reports **0
advisory violations** — no core/domain header outside the two named adapters
includes Qt.

---

## 5.2 Thread-safety audit (singleton core services)

All four target services are **Meyers singletons** (`static X& instance()` →
`static X inst;` inside the function). C++11 guarantees thread-safe local-static
initialization, so construction is safe. Audit of runtime locking:

| Service | Singleton | Internal locking | Hot-path counters | Verdict |
|---------|-----------|------------------|-------------------|---------|
| `CacheManager` (`core/cache/CacheManager.h`) | ✓ | `mutable std::mutex m_metaMutex` guards all metadata access (cpp:128/163/181/191/202) | `m_hits`/`m_misses` are `std::atomic` (lock-free) | **SAFE** |
| `TaskScheduler` (`core/scheduler/TaskScheduler.h`) | ✓ | `mutable std::mutex m_graphMtx` guards task-graph state | `s_nextId` is `std::atomic<uint64_t>` | **SAFE** |
| `ImageRepository` (`core/image/ImageRepository.h`) | ✓ | delegates locking to inner `ImageCache` (`pool.mtx`) + `DiskCache` (`s_createMutex` QMutex) — no locking in `ImageRepository.cpp` itself | n/a | **SAFE by delegation** |
| `MemoryTracker` (`core/perf/MemoryTracker.h`) | ✓ | `mutable std::mutex m_peakMtx` guards peak/RSS tracking | frame counters `std::atomic` | **SAFE** |

**Lifecycle / shutdown**: singletons live for process duration; no explicit
`shutdown()` is required because they hold only memory caches and atomics — they
are destroyed at static-exit with no cross-thread ordering hazard (all worker
threads are joined by `TaskScheduler` before process exit via its pool teardown).
No use-after-free or double-free path identified.

**Findings**: none blocking. Two minor notes (non-blocking, deferred):
- `CacheManager` metadata mutex is a single coarse lock; under pathological
  >50k-entry churn it could contend, but measured B6 RSS stayed at 570 MB @1000
  images (M12.2), so no production pressure observed.
- `DiskCache` uses a function-local `static QMutex` (cpp:97) — fine, but a
  `static` Meyers-style wrapper would be more idiomatic than a function-local
  static; cosmetic only.

**Conclusion**: thread-safety posture is sound; no code change required for M12.5.

---

## 5.3 API documentation skeleton (public core surface)

The four headline APIs get a stable reference page each under `docs/api/`. These
are **interface contracts** (what callers rely on), not implementation docs.
Status: skeleton committed; full prose to be filled as part of the M12.5
follow-up (or post-1.0 doc sprint). Files created:

- `docs/api/ImageRepository.md` — `load()`, `loadAsync()`, `LoadOptions`, cache
  level, cancellation handle.
- `docs/api/Decoder.md` — decoder registry, format support matrix (JPEG/PNG/
  TIFF/BMP/WebP), decode options, error contract.
- `docs/api/Analyzer.md` — `IAnalyzer`, `AnalyzerRegistry` (`registerAnalyzer` /
  `getAnalyzer` / `runAnalyzer`), built-in analyzers (Histogram, RGB Mean, PSNR,
  SSIM, Sharpness, Noise); Qt-free (no QWidget/QPainter dependency).
- `docs/api/RenderEngine.md` — `Renderer` interface, `SoftwareRenderer`,
  `RenderCommand` pipeline, `RenderEngine` facade; states the `QPainter`
  boundary in the API (no Qt include in the header — see 5.1 and ADR 016).

See those files for the per-API contract. The analyzer Registry is the most
product-critical (review P1): `core/analyzer/` must expose `registerAnalyzer()` /
`getAnalyzer()` / `runAnalyzer()` and must NOT depend on Qt UI types.

---

## Verification

- `scripts/audit_qt_boundary.ps1` → **138 headers scanned, 0 forbidden, 1
  warning** (1 sanctioned ALLOWED: `QtConvert.h`; the warning is
  `QtMetadataSemantics.h`, the second R5 adapter, which this scanner's older
  allow-list does not name). Exit 0.
- `scripts/architecture_gate.ps1` (R1–R5) → **0 advisory violations** at HEAD, so
  no core/domain header outside the two named Qt adapters includes Qt.
- `build.ps1 Test` → **this number needs regeneration.** The suite now registers
  **132** tests (132 `add_test(NAME ...)` calls across `CMakeLists.txt` +
  `src/CMakeLists.txt`), so the "26/27 CTest pass" figure below is from the M12.5
  era and cannot be quoted as current; re-read it from a fresh
  `.\build.ps1 Test` / `ctest` run. The historical note it carried
  (`bench_smoke` Windows Qt-plugin teardown crash, ALL PASS on direct run) is
  likewise unverified at HEAD.
- Thread-safety conclusions drawn from source inspection of the four singleton
  headers + their `.cpp` locking sites (see 5.2 table).

## Out of scope / deferred

- Moving `QPainter` out of `RenderEngine.h` (would breach the RenderEngine
  freeze; deferred to a post-1.0 UI-adapter refactor if ever needed).
- Doc prose expansion for `docs/api/*` (skeleton only this milestone).
- `bench_smoke` CTest teardown fix (would touch the ctest wrapper; environmental).
